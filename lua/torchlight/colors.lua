-- The default palette, kept at its original path so `require` calls in user
-- config and in the specs keep working. Variants load through
-- `torchlight.palettes`.
return require('torchlight.palettes').get('torchlight')
